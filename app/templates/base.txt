<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BUKABOX — Financial Intelligence Dashboard v4.3-Sync</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='layout.css') }}">
  <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
  
</head>
<body>
  <!-- Header -->
<header class="navbar">
  <div class="content-wrap nav-inner">
    <!-- Kiri: Logo -->
    <div class="nav-left">
      <span class="logo">💼 BUKABOX <small>— Financial Intelligence Dashboard</small></span>
    </div>

    <!-- Tombol Burger -->
    <button class="nav-toggle" id="navToggle" aria-label="toggle menu" aria-expanded="false">☰</button>

    <!-- Menu Navigasi -->
    <nav class="nav-menu" id="navMenu">
      <a href="{{ url_for('index') }}" class="btn-nav active">Dashboard</a>
      <a class="btn-nav" href="{{ url_for('investment_panel') }}">Investment</a>
      {% if not session.get('logged_in') %}
        <a href="#" id="loginBtn" class="btn-nav">Login</a>
      {% endif %}
      <a class="btn-nav" href="{{ url_for('save_month_snapshot') }}">Rekap Bulanan</a>
      <div class="user-banner">
  <span>👤 {{ session['username'] }}</span>
  <a href="{{ url_for('logout') }}" title="Logout" style="margin-left:6px;">
  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
    <polyline points="16 17 21 12 16 7"/>
    <line x1="21" y1="12" x2="9" y2="12"/>
  </svg>
</a>
</div>
    </nav>
  </div>
</header>
<div class="layout">
    <!-- ===== SIDEBAR ===== -->
    <aside class="sidebar">
      <div class="sidebar-header">
        <h2>BUKABOX</h2>
      </div>
      <nav class="menu">
        <a href="{{ url_for('index') }}" class="menu-item active">🏠 Dashboard</a>
        <a href="{{ url_for('history') }}" class="menu-item">📅 History</a>
        <a href="{{ url_for('investment_panel') }}" class="menu-item">💰 Investment</a>
        <a href="{{ url_for('expense_panel') }}" class="menu-item">💸 Expenses</a>
        <a href="{{ url_for('income_panel') }}" class="menu-item">📈 Income</a>
        <a href="{{ url_for('emergency_panel') }}" class="menu-item">🩺 Emergency Fund</a>
      </nav>
      <div class="sidebar-footer">
        <small>v4.6</small>
      </div>
    </aside>
  <main class="main-content">
    {% block content %}
    {% endblock %}
  </main>

  </div>
  <footer>
    <p>© 2025 PT BUKABOX CREATIVE STUDIO</p>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="{{ url_for('static', filename='js/main.js') }}"></script>

</body>
</html>
